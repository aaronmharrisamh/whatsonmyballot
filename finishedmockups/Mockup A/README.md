# Mockup A

Open [A5](index.html?design=a5) through the repository's HTTP preview. All five designs share this app.

The [complete handoff](../../sharedrefs/mockup-a/README.md), [review worksheet](../../sharedrefs/mockup-a/REVIEW.md), [content guide](../../sharedrefs/mockup-a/CONTENT.md), and [hosting notes](../../sharedrefs/mockup-a/HOSTING.md) are in `/sharedrefs/`.

Keep this folder inside the public project layout, alongside the root `sharedrefs/` folder and `version.js`. Run `node preview.mjs` from the repository root.
