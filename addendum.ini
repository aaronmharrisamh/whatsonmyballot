[release_hold]
status = active
public_mockup = B.9A
withheld_mockups = B.9B,B.9C,B.9D,B.9E,B.9F,B.9G,B.9H,B.9I-TEST,B.9J
local_archive = unreleasedmockups/

[git_history_cleanup]
status = pending
requested_follow_up = Remove previously published withheld mockups and related artifacts from public Git history.
scope = Later mockup folders, review files, demo examples, and ZIP archives containing those versions.
current_decision = Generated packages are retired; keep existing Git history intact until a separate review of the proposed reset.
preparation = Back up the complete repository and local archive; identify affected branches, tags, releases, and other published copies; coordinate with collaborators.
execution = Obtain separate authorization for history rewriting and any force push; remove the identified paths from the relevant history.
verification = Check rewritten refs and current Pages output; arrange fresh clones as needed. Existing downloads, forks, and outside caches cannot be recalled by rewriting this repository.
